import { Avatar } from './avatar.directive';

describe('AvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new Avatar();
    expect(directive).toBeTruthy();
  });
});
