import { InputRadioDirective } from './input-radio.directive';

describe('InputRadioDirective', () => {
  it('should create an instance', () => {
    const directive = new InputRadioDirective();
    expect(directive).toBeTruthy();
  });
});
