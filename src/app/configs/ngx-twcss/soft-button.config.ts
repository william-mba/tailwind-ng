import { SoftButtonConfig } from "ngx-twcss";

export const CustomSoftButtonConfig: SoftButtonConfig = {
  theme: {
    textColor: 'text-rose-600',
    bgColor: 'bg-rose-600'
  }
}

export const CustomSoftButtonConfig2: SoftButtonConfig = {
  theme: {
    textColor: 'text-orange-600',
    bgColor: 'bg-orange-600'
  }
}

export const CustomSoftButtonConfig3: SoftButtonConfig = {
  theme: {
    textColor: 'text-lime-600',
    bgColor: 'bg-lime-600'
  }
}
