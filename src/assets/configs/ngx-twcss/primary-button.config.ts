import { PrimaryButtonConfig } from "ngx-twcss"

export const CustomPrimaryButtonConfig: PrimaryButtonConfig = {
  theme: {
    bgColor: 'bg-rose-500'
  }
}

export const CustomPrimaryButtonConfig2: PrimaryButtonConfig = {
  theme: {
    bgColor: 'bg-orange-600'
  }
}

export const CustomPrimaryButtonConfig3: PrimaryButtonConfig = {
  theme: {
    bgColor: 'bg-lime-600'
  }
}
